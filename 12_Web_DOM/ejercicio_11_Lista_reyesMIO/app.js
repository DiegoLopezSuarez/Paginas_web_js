class App {
    constructor () {
        
        this.textoInput = document.querySelector('.rellenaNombre')
        this.btnConfirma = document.querySelector('.confirmar')
    
        this.btnConfirma.addEventListener('click', this.mostrar.bind(this))
    }

    nombreReyes = [Fernando, Carlos, Felipe, Amadeo, Alfonso, Juan Carlos]

    Function (confirmarNombre)
    let acierto = nombreCorrecto

    if acierto == array.forEach(nombreReyes => {
        
    });

    mostrar(nombreCorrecto) {  'tu respuesta es correcta'
        acierto = 
        console.log(correcta.target)


document.addEventListener('DOMContentLoaded',
        () => { new App()})